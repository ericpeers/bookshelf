<%= form_for @book do |f| %>
<% if @book.errors.any? %>
<div id="error_splaining">
  <!-- fully lifted from the ruby tutorial. Far better would be to inline each error next to the field that caused it. Now how do I do that? FIXME -->
  <h2>
    <%= pluralize(@book.errors.count, "book") %> prevented this book from being saved:
  </h2>
  <ul>
    <% @book.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %> <!--book errors -->

<p>
  <%= f.label :author %><br>
  <%= f.text_field :author %>
</p>
<p>
  <%= f.label :title %><br>
  <%= f.text_field :title %>
</p>
<p>
  <%= f.label :isbn %><br>
  <%= f.text_field :isbn %>
</p>

<p><%= f.submit %></p>
<% end %> <!-- form_for -->

<%= link_to 'Go back to book listings', books_path %>
